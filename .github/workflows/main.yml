name: Windows
on:
  workflow_dispatch:
   inputs:
     code: 
       required: True
     Name:
       required: False
     AndroidStudio:
       type: boolean
     DevX:
       type: boolean
     Bluestacks:
       type: boolean

jobs:
  windows:
    name: Windows 10
    runs-on: windows-2022

    steps:
    - name: setup
      run: | 
       $temp = $Env:COMPUTERNAME
       $Env:COMPUTERNAME = ${{ inputs.Name }}
       choco install chrome-remote-desktop-host --ignore-checksum
       ${{ inputs.code }} --pin=123456
       $Env:COMPUTERNAME = $temp
    - name: Installing Tools
      shell: pwsh
      run: |
        if ("${{ inputs.AndroidStudio }}" -eq "true") { choco install androidstudio --ignore-checksum}
        if ("${{ inputs.DevX }}" -eq "true") { write-host " Devx installation"}
        if ("${{ inputs.Bluestacks }}" -eq "true") { choco install bluestacks --ignore-checksum}
    - name: CountDown
      run: |
           $i = 355
           do {
             Write-Host $i
             Sleep 60
             $i--
           } while ($i -gt 0)
